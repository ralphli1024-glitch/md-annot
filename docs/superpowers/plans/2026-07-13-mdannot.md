# MDAnnot Obsidian 插件 实现计划

> **工作者：** 用 tech_loop_exec_plan 逐任务执行。

## 计划元数据

- **目标：** 实现 MDAnnot Obsidian 插件——在 Markdown 文档上叠加手写批注层，批注锚定到文本内容，不破坏 `.md` 原文
- **技能引用：** tech_loop_coder
- **范围边界（不在此计划内）：**
  - 不实现导出所有文档批注（仅在侧面板支持导出当前文档）
  - 不实现 Obsidian 社区插件商店发布流程
  - 不实现 OCR/图片文字识别
  - 不实现 iPad 手势快捷键定制
- **关键假设：** 开发环境为 macOS。插件通过 `npm run dev` 热重载开发，通过 `obsidian-plugin-dev` 或手动复制到 vault 测试

## 架构

插件采用模块化架构，核心模块通过 EventEmitter 通信：

```
main.ts (插件入口 / 生命周期 / 视图切换)
  ├─ annotation-manager.ts (核心状态管理 / CRUD / 事件总线)
  │   ├─ data-models.ts (类型定义 / 枚举)
  │   ├─ storage.ts (文件读写 / 历史版本)
  │   └─ anchor-engine.ts (指纹计算 / 匹配降级)
  ├─ floating-toolbar.ts (编辑模式选中工具栏)
  ├─ cm6-decorations.ts (CM6 装饰渲染器)
  ├─ side-panel.ts (右侧批注列表面板)
  ├─ canvas-overlay.ts (阅读模式画布覆盖层)
  │   └─ handwriting-engine.ts (笔触渲染 / Catmull-Rom)
  └─ settings-tab.ts (设置面板)
```

关键调用链：
1. 编辑模式：选中文字 → FloatingToolbar → AnnotationManager.create() → Storage.save() → emit → CM6 更新装饰
2. 切换文件/编辑内容 → AnnotationManager.reanchor() → AnchorEngine 重算 → CM6 更新
3. 阅读模式：CanvasOverlay 捕获指针事件 → HandwritingEngine.render() → 保存到 Storage

## 技术栈

| 项 | 版本/工具 |
|----|----------|
| 语言 | TypeScript 5.x |
| 构建 | esbuild (obsidian 模板) |
| 运行时 | Obsidian API (obsidian 1.5+) |
| 编辑器 | CodeMirror 6 (通过 obsidian 封装) |
| 测试 | vitest (核心逻辑单元测试) |
| 包管理 | npm / yarn |

## 共享类型与契约

### 关键枚举

```typescript
enum AnnotationType {
  HIGHLIGHT = 'highlight',
  UNDERLINE = 'underline',
  COMMENT = 'comment',
  HANDWRITING = 'handwriting'
}

enum AnchorStatus {
  EXACT = 'exact',
  FUZZY = 'fuzzy',
  PARAGRAPH = 'paragraph',
  LOST = 'lost'
}

enum StrokeTool {
  PEN = 'pen',
  HIGHLIGHTER = 'highlighter',
  ERASER = 'eraser'
}
```

### 核心接口

```typescript
interface Point {
  x: number;
  y: number;
  pressure: number;
  timestamp: number;
}

interface Stroke {
  id: string;
  points: Point[];
  color: string;
  width: number;
  tool: StrokeTool;
}

interface Annotation {
  id: string;
  type: AnnotationType;
  targetText: string;
  contextBefore: string;
  contextAfter: string;
  fingerprint: string;
  color?: string;
  commentText?: string;
  strokes?: Stroke[];
  // 运行时计算（不持久化）
  anchorStatus: AnchorStatus;
  startLine?: number;
  startCh?: number;
  endLine?: number;
  endCh?: number;
  createdAt: number;
  updatedAt: number;
}

interface AnnotationFile {
  version: number;
  filePath: string;
  annotations: SerializedAnnotation[];
  handwriting?: { strokes: Stroke[]; width: number; height: number };
  updatedAt: number;
}

interface SerializedAnnotation {
  id: string;
  type: AnnotationType;
  targetText: string;
  contextBefore: string;
  contextAfter: string;
  fingerprint: string;
  color?: string;
  commentText?: string;
  strokes?: Stroke[];
  createdAt: number;
  updatedAt: number;
}
```

### 事件总线

```typescript
interface AnnotationEvents {
  'annotation:created': Annotation;
  'annotation:updated': Annotation;
  'annotation:deleted': { id: string };
  'annotations:reanchored': Annotation[];
  'file:changed': { path: string };
}
```

### 设置接口

```typescript
interface MDAnnotSettings {
  // 显示控制
  showInEditor: boolean;
  showInReading: boolean;
  autoShowPanel: boolean;
  
  // 批注样式
  highlightColor: string;
  underlineColor: string;
  commentHighlightColor: string;
  
  // 手写输入
  defaultPenColor: string;
  defaultPenWidth: number;
  defaultHighlighterWidth: number;
  smoothness: number;
  
  // 数据存储
  keepHistory: boolean;
  maxHistoryCount: number;
  autoSaveInterval: number;
  
  // Markdown 互操作
  syncHighlightToMD: boolean;
  syncUnderlineToMD: boolean;
}

const DEFAULT_SETTINGS: MDAnnotSettings = {
  showInEditor: true,
  showInReading: true,
  autoShowPanel: true,
  highlightColor: '#90EE90',
  underlineColor: '#4169E1',
  commentHighlightColor: '#FFFFAA',
  defaultPenColor: '#FF4500',
  defaultPenWidth: 2,
  defaultHighlighterWidth: 12,
  smoothness: 0.5,
  keepHistory: true,
  maxHistoryCount: 20,
  autoSaveInterval: 30,
  syncHighlightToMD: false,
  syncUnderlineToMD: false,
};
```

## 任务依赖关系

```
- **Task 1** → 无依赖（首执行）
- **Task 2** → [Task 1] 需要项目结构存在
- **Task 3** → [Task 2] 需要数据模型存在
- **Task 4** → [Task 2, Task 3] 需要存储和锚定
- **Task 5** → [Task 4] 需要 AnnotationManager
- **Task 6** → [Task 4] 需要 AnnotationManager（可并行于 Task 5）
- **Task 7** → [Task 4, Task 1] 需要 AnnotationManager 和项目结构
- **Task 8** → [Task 1, Task 5, Task 6, Task 7] 集成所有模块
- **Task 9** → [Task 1] 可随时创建

并行组: [Task 1] → [Task 2] → [Task 3, Task 9*] → [Task 4] → [Task 5, Task 6, Task 7] → [Task 8]
(*Task 9 可以在任何时候创建，仅依赖 Task 1)
```

## 上下文预读

```
1. `需求/MDAnnot需求文档.md` — 整体需求理解
2. `技术方案/MDAnnot技术方案.md` — 完整接口签名、算法伪代码、存储结构
3. Obsidian Sample Plugin: https://github.com/obsidianmd/obsidian-sample-plugin — 项目模板参考
```

---

## 任务总览

| # | 任务 | 文件范围 | TDD | 依赖 |
|---|------|---------|-----|------|
| 1 | 项目脚手架 | package.json + tsconfig + esbuild + manifest.json + styles.css骨架 + 目录 | 编译验证 | - |
| 2 | 数据模型 + StorageService | src/data-models.ts, src/storage.ts | 是 | [1] |
| 3 | AnchorEngine | src/anchor-engine.ts | 是 | [2] |
| 4 | AnnotationManager | src/annotation-manager.ts | 是 | [2,3] |
| 5 | 编辑模式：FloatingToolbar + CM6装饰 | src/floating-toolbar.ts, src/cm6-decorations.ts | 编译验证 | [4] |
| 6 | 侧面板：SidePanel | src/side-panel.ts | 编译验证 | [4] |
| 7 | 阅读模式：CanvasOverlay + HandwritingEngine | src/canvas-overlay.ts, src/handwriting-engine.ts | 编译验证 | [4] |
| 8 | SettingsTab + main.ts 集成 | src/settings-tab.ts, src/main.ts | 编译验证 | [1,5,6,7] |
| 9 | CSS 样式 | styles.css | - | [1] |

---

### 任务 1：项目脚手架

**文件：** 创建: `package.json`, `tsconfig.json`, `esbuild.config.mjs`, `manifest.json`, `src/main.ts` (骨架)

**定位锚点：** 新文件，无需锚点

**关键决策：** Obsidian 插件的版本号在 manifest.json 中定义，与 package.json 同步。

- [ ] **Step1: 创建 package.json**

```json
{
  "name": "md-annot",
  "version": "0.1.0",
  "description": "Markdown handwriting annotation plugin for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run"
  },
  "keywords": ["obsidian", "plugin", "annotation", "handwriting"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.19.0",
    "obsidian": "^1.5.0",
    "tslib": "^2.6.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
```

- [ ] **Step2: 创建 tsconfig.json**

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "inlineSourceMap": true,
    "inlineSources": true,
    "module": "ESNext",
    "target": "ES6",
    "allowJs": true,
    "noImplicitAny": true,
    "moduleResolution": "node",
    "importHelpers": true,
    "isolatedModules": true,
    "strictNullChecks": true,
    "lib": ["DOM", "ES5", "ES6", "ES7"],
    "allowSyntheticDefaultImports": true
  },
  "include": ["src/**/*.ts"]
}
```

- [ ] **Step3: 创建 esbuild.config.mjs**

```javascript
import esbuild from "esbuild";
import process from "process";
import builtins from "builtin-modules";

const banner = `/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
*/
`;

const prod = process.argv[2] === "production";

const context = await esbuild.context({
  banner: { js: banner },
  entryPoints: ["src/main.ts"],
  bundle: true,
  external: [
    "obsidian",
    "electron",
    "@codemirror/autocomplete",
    "@codemirror/collab",
    "@codemirror/commands",
    "@codemirror/language",
    "@codemirror/lint",
    "@codemirror/search",
    "@codemirror/state",
    "@codemirror/view",
    "@codemirror/stream-parser",
    ...builtins,
  ],
  format: "cjs",
  target: "es2018",
  logLevel: "info",
  sourcemap: prod ? false : "inline",
  treeShaking: true,
  outfile: "main.js",
});

if (prod) {
  await context.rebuild();
  process.exit(0);
} else {
  await context.watch();
}
```

- [ ] **Step4: 创建 manifest.json**

```json
{
  "id": "md-annot",
  "name": "MDAnnot",
  "version": "0.1.0",
  "minAppVersion": "1.5.0",
  "description": "Handwrite annotations on Markdown documents",
  "author": "",
  "authorUrl": "",
  "isDesktopOnly": false
}
```

- [ ] **Step5: 创建 src/main.ts 骨架**

```typescript
import { App, Plugin, PluginSettingTab, Setting } from 'obsidian';

export default class MDAnnotPlugin extends Plugin {
  async onload() {
    console.log('MDAnnot: loaded');
  }

  async onunload() {
    console.log('MDAnnot: unloaded');
  }
}
```

- [ ] **Step6: 创建 src/styles.css 空骨架**

```css
/* MDAnnot Styles */
```

- [ ] **Step7: 创建 src/ 目录**

```bash
mkdir -p src/ __tests__/
```

- [ ] **Step8: 安装依赖并编译验证**

```bash
npm install
npx tsc --noEmit --skipLibCheck
```
预期：无编译错误

- [ ] **Step9: 提交**

```bash
git add -A && git commit -m "chore: 项目脚手架"
```

---

### 任务 2：数据模型 + StorageService

**文件：** 创建: `src/data-models.ts`, `src/storage.ts`  
测试: `__tests__/storage.test.ts`

**定位锚点：** 新文件，无需锚点

**关键决策：** StorageService 使用 `app.vault.adapter` 操作文件系统。因为插件在 Obsidian 内运行，单元测试中使用 mock。

**Task 2a: 数据模型**

- [ ] **Step1: 写测试**

```typescript
// __tests__/data-models.test.ts
import { describe, it, expect } from 'vitest';
import { AnnotationType, AnchorStatus, StrokeTool } from '../src/data-models';

describe('Data Models Enums', () => {
  it('should have correct AnnotationType values', () => {
    expect(AnnotationType.HIGHLIGHT).toBe('highlight');
    expect(AnnotationType.UNDERLINE).toBe('underline');
    expect(AnnotationType.COMMENT).toBe('comment');
    expect(AnnotationType.HANDWRITING).toBe('handwriting');
  });

  it('should have correct AnchorStatus values', () => {
    expect(AnchorStatus.EXACT).toBe('exact');
    expect(AnchorStatus.FUZZY).toBe('fuzzy');
    expect(AnchorStatus.PARAGRAPH).toBe('paragraph');
    expect(AnchorStatus.LOST).toBe('lost');
  });

  it('should have correct StrokeTool values', () => {
    expect(StrokeTool.PEN).toBe('pen');
    expect(StrokeTool.HIGHLIGHTER).toBe('highlighter');
    expect(StrokeTool.ERASER).toBe('eraser');
  });
});
```

- [ ] **Step2: 跑测试**

```bash
npx vitest run __tests__/data-models.test.ts
```
预期：FAIL（文件不存在）

- [ ] **Step3: 创建 src/data-models.ts**

```typescript
export enum AnnotationType {
  HIGHLIGHT = 'highlight',
  UNDERLINE = 'underline',
  COMMENT = 'comment',
  HANDWRITING = 'handwriting',
}

export enum AnchorStatus {
  EXACT = 'exact',
  FUZZY = 'fuzzy',
  PARAGRAPH = 'paragraph',
  LOST = 'lost',
}

export enum StrokeTool {
  PEN = 'pen',
  HIGHLIGHTER = 'highlighter',
  ERASER = 'eraser',
}

export interface Point {
  x: number;
  y: number;
  pressure: number;
  timestamp: number;
}

export interface Stroke {
  id: string;
  points: Point[];
  color: string;
  width: number;
  tool: StrokeTool;
}

export interface Annotation {
  id: string;
  type: AnnotationType;
  targetText: string;
  contextBefore: string;
  contextAfter: string;
  fingerprint: string;
  color?: string;
  commentText?: string;
  strokes?: Stroke[];
  anchorStatus: AnchorStatus;
  startLine?: number;
  startCh?: number;
  endLine?: number;
  endCh?: number;
  createdAt: number;
  updatedAt: number;
}

export interface SerializedAnnotation {
  id: string;
  type: AnnotationType;
  targetText: string;
  contextBefore: string;
  contextAfter: string;
  fingerprint: string;
  color?: string;
  commentText?: string;
  strokes?: Stroke[];
  createdAt: number;
  updatedAt: number;
}

export function serializeAnnotation(anno: Annotation): SerializedAnnotation {
  const { anchorStatus, startLine, startCh, endLine, endCh, ...rest } = anno;
  return rest;
}

export function deserializeAnnotation(
  serialized: SerializedAnnotation
): Annotation {
  return {
    ...serialized,
    anchorStatus: AnchorStatus.LOST,
    startLine: undefined,
    startCh: undefined,
    endLine: undefined,
    endCh: undefined,
  };
}

export interface AnnotationFile {
  version: number;
  filePath: string;
  annotations: SerializedAnnotation[];
  handwriting?: {
    strokes: Stroke[];
    width: number;
    height: number;
  };
  updatedAt: number;
}

export interface AnnotationIndex {
  files: Record<
    string,
    { count: number; updatedAt: number; hasHandwriting: boolean }
  >;
}

export function generateId(): string {
  return Date.now().toString(36) + Math.random().toString(36).substring(2, 9);
}

export function sanitizeFileName(filePath: string): string {
  return filePath.replace(/\.md$/i, '').replace(/[\/\\:]/g, '__');
}

export interface MDAnnotSettings {
  showInEditor: boolean;
  showInReading: boolean;
  autoShowPanel: boolean;
  highlightColor: string;
  underlineColor: string;
  commentHighlightColor: string;
  defaultPenColor: string;
  defaultPenWidth: number;
  defaultHighlighterWidth: number;
  smoothness: number;
  keepHistory: boolean;
  maxHistoryCount: number;
  autoSaveInterval: number;
  syncHighlightToMD: boolean;
  syncUnderlineToMD: boolean;
}

export const DEFAULT_SETTINGS: MDAnnotSettings = {
  showInEditor: true,
  showInReading: true,
  autoShowPanel: true,
  highlightColor: '#90EE90',
  underlineColor: '#4169E1',
  commentHighlightColor: '#FFFFAA',
  defaultPenColor: '#FF4500',
  defaultPenWidth: 2,
  defaultHighlighterWidth: 12,
  smoothness: 0.5,
  keepHistory: true,
  maxHistoryCount: 20,
  autoSaveInterval: 30,
  syncHighlightToMD: false,
  syncUnderlineToMD: false,
};
```

- [ ] **Step4: 跑测试**

```bash
npx vitest run __tests__/data-models.test.ts
```
预期：PASS

**Task 2b: StorageService**

- [ ] **Step5: 创建 src/storage.ts**

```typescript
import { Vault } from 'obsidian';
import {
  AnnotationFile,
  AnnotationIndex,
  SerializedAnnotation,
  Annotation,
  deserializeAnnotation,
  sanitizeFileName,
} from './data-models';

const ANNOTATIONS_DIR = '.annotations';
const CURRENT_VERSION = 1;

export class StorageService {
  private vault: Vault;
  private indexCache: AnnotationIndex | null = null;

  constructor(vault: Vault) {
    this.vault = vault;
  }

  private getJsonPath(filePath: string): string {
    return `${ANNOTATIONS_DIR}/${sanitizeFileName(filePath)}.current.json`;
  }

  private getHistoryDir(filePath: string): string {
    return `${ANNOTATIONS_DIR}/${sanitizeFileName(filePath)}.history`;
  }

  async ensureDir(dirPath: string): Promise<void> {
    const exists = await this.vault.adapter.exists(dirPath);
    if (!exists) {
      await this.vault.adapter.mkdir(dirPath);
    }
  }

  async loadFile(filePath: string): Promise<Annotation | null> {
    const data = await this.load(filePath);
    return data ? (data.annotations.length > 0 ? deserializeAnnotation(data.annotations[0]) : null) : null;
  }

  async load(filePath: string): Promise<AnnotationFile | null> {
    const jsonPath = this.getJsonPath(filePath);
    const exists = await this.vault.adapter.exists(jsonPath);
    if (!exists) return null;

    const content = await this.vault.adapter.read(jsonPath);
    return JSON.parse(content) as AnnotationFile;
  }

  async save(
    filePath: string,
    annotations: SerializedAnnotation[],
    handwriting?: AnnotationFile['handwriting']
  ): Promise<void> {
    await this.ensureDir(ANNOTATIONS_DIR);

    const data: AnnotationFile = {
      version: CURRENT_VERSION,
      filePath,
      annotations,
      handwriting,
      updatedAt: Date.now(),
    };

    const jsonPath = this.getJsonPath(filePath);
    await this.vault.adapter.write(jsonPath, JSON.stringify(data, null, 2));
  }

  async saveWithHistory(
    filePath: string,
    annotations: SerializedAnnotation[],
    handwriting?: AnnotationFile['handwriting'],
    maxHistoryCount: number = 20
  ): Promise<void> {
    // 先保存当前版本
    await this.save(filePath, annotations, handwriting);

    // 保存历史
    const historyDir = this.getHistoryDir(filePath);
    await this.ensureDir(historyDir);

    const timestamp = new Date().toISOString().replace(/[:.]/g, '').slice(0, 15);
    const historyPath = `${historyDir}/${timestamp}.json`;

    const data: AnnotationFile = {
      version: CURRENT_VERSION,
      filePath,
      annotations,
      handwriting,
      updatedAt: Date.now(),
    };
    await this.vault.adapter.write(historyPath, JSON.stringify(data, null, 2));

    // 清理历史
    await this.cleanupHistory(historyDir, maxHistoryCount);
  }

  private async cleanupHistory(dir: string, maxCount: number): Promise<void> {
    const files = (await this.vault.adapter.list(dir)).files.sort();
    while (files.length > maxCount) {
      const oldest = files.shift();
      if (oldest) {
        await this.vault.adapter.remove(oldest);
      }
    }
  }

  async getAllAnnotations(filePath: string): Promise<Annotation[]> {
    const data = await this.load(filePath);
    if (!data) return [];
    return data.annotations.map(deserializeAnnotation);
  }

  async saveAnnotations(
    filePath: string,
    annotations: Annotation[],
    maxHistoryCount?: number
  ): Promise<void> {
    const serialized = annotations.map((a) => {
      const { anchorStatus, startLine, startCh, endLine, endCh, ...rest } = a;
      return rest;
    });

    if (maxHistoryCount !== undefined) {
      await this.saveWithHistory(filePath, serialized, undefined, maxHistoryCount);
    } else {
      await this.save(filePath, serialized);
    }
  }
}
```

- [ ] **Step6: 跑编译验证**

```bash
npx tsc --noEmit --skipLibCheck
```
预期：无编译错误

- [ ] **Step7: 提交**

```bash
git add -A && git commit -m "feat: 数据模型与存储服务"
```

---

### 任务 3：AnchorEngine

**文件：** 创建: `src/anchor-engine.ts`  
测试: `__tests__/anchor-engine.test.ts`

**定位锚点：** 新文件，无需锚点

- [ ] **Step1: 写测试**

```typescript
// __tests__/anchor-engine.test.ts
import { describe, it, expect } from 'vitest';
import {
  computeFingerprint,
  fnv1aHash,
  levenshteinSimilarity,
  findAnnotationPosition,
  AnchorMatchResult,
} from '../src/anchor-engine';
import { Annotation, AnnotationType, AnchorStatus } from '../src/data-models';

describe('fnv1aHash', () => {
  it('should produce consistent hash for same input', () => {
    const h1 = fnv1aHash('hello world');
    const h2 = fnv1aHash('hello world');
    expect(h1).toBe(h2);
  });

  it('should produce different hash for different input', () => {
    const h1 = fnv1aHash('hello');
    const h2 = fnv1aHash('world');
    expect(h1).not.toBe(h2);
  });

  it('should return string', () => {
    const h = fnv1aHash('test');
    expect(typeof h).toBe('string');
    expect(h.length).toBeGreaterThan(0);
  });
});

describe('computeFingerprint', () => {
  it('should produce hash from context + target', () => {
    const fp = computeFingerprint('target', 'before', 'after');
    expect(fp).toBeTruthy();
    expect(typeof fp).toBe('string');
  });

  it('should be deterministic', () => {
    const fp1 = computeFingerprint('same', 'ctx', 'aft');
    const fp2 = computeFingerprint('same', 'ctx', 'aft');
    expect(fp1).toBe(fp2);
  });
});

describe('levenshteinSimilarity', () => {
  it('should return 1 for identical strings', () => {
    expect(levenshteinSimilarity('abc', 'abc')).toBe(1);
  });

  it('should return 0 for completely different strings', () => {
    expect(levenshteinSimilarity('abc', 'xyz')).toBe(0);
  });

  it('should return > 0.7 for similar strings', () => {
    const score = levenshteinSimilarity('hello world', 'hello world!');
    expect(score).toBeGreaterThan(0.9);
  });

  it('should handle empty strings', () => {
    expect(levenshteinSimilarity('', '')).toBe(1);
    expect(levenshteinSimilarity('abc', '')).toBe(0);
  });
});

describe('findAnnotationPosition', () => {
  const docContent = `# Title
This is a paragraph with some text.
Another line here.
Yet another paragraph with important content.
Final line.`;

  const exactAnnotation: Annotation = {
    id: 'test1',
    type: AnnotationType.HIGHLIGHT,
    targetText: 'paragraph with some text',
    contextBefore: '# Title\nThis is a',
    contextAfter: '.',
    fingerprint: '',
    anchorStatus: AnchorStatus.LOST,
    createdAt: Date.now(),
    updatedAt: Date.now(),
  };

  it('should find exact match', () => {
    const result = findAnnotationPosition(exactAnnotation, docContent);
    expect(result.status).toBe(AnchorStatus.EXACT);
    expect(result.score).toBe(1);
  });

  it('should find fuzzy match when text slightly changed', () => {
    const fuzzyAnno: Annotation = {
      ...exactAnnotation,
      targetText: 'paragraph with sme text', // typo
      contextBefore: '',
      contextAfter: '',
    };
    const result = findAnnotationPosition(fuzzyAnno, docContent);
    expect(result.status).toBe(AnchorStatus.FUZZY);
    expect(result.score).toBeGreaterThan(0.7);
  });

  it('should return paragraph level match for unchanged content', () => {
    const paraAnno: Annotation = {
      id: 'test2',
      type: AnnotationType.HIGHLIGHT,
      targetText: 'completely missing text that never existed',
      contextBefore: '',
      contextAfter: '',
      fingerprint: '',
      anchorStatus: AnchorStatus.LOST,
      createdAt: Date.now(),
      updatedAt: Date.now(),
    };
    const result = findAnnotationPosition(paraAnno, docContent);
    expect(result.status).toBe(AnchorStatus.PARAGRAPH);
  });

  it('should return lost for empty doc', () => {
    const result = findAnnotationPosition(exactAnnotation, '');
    expect(result.status).toBe(AnchorStatus.LOST);
  });
});
```

- [ ] **Step2: 跑测试**

```bash
npx vitest run __tests__/anchor-engine.test.ts
```
预期：FAIL（文件不存在）

- [ ] **Step3: 创建 src/anchor-engine.ts**

```typescript
import { Annotation, AnchorStatus } from './data-models';

export interface AnchorMatchResult {
  status: AnchorStatus;
  startLine: number;
  startCh: number;
  endLine: number;
  endCh: number;
  score: number;
}

export function fnv1aHash(str: string): string {
  let hash = 0x811c9dc5;
  for (let i = 0; i < str.length; i++) {
    hash ^= str.charCodeAt(i);
    hash = Math.imul(hash, 0x01000193);
    hash >>>= 0;
  }
  return hash.toString(36);
}

export function computeFingerprint(
  targetText: string,
  contextBefore: string,
  contextAfter: string
): string {
  const content = `${contextBefore.slice(-100)}|||${targetText}|||${contextAfter.slice(0, 100)}`;
  return fnv1aHash(content);
}

export function levenshteinDistance(a: string, b: string): number {
  const m = a.length;
  const n = b.length;
  const dp: number[][] = [];

  for (let i = 0; i <= m; i++) {
    dp[i] = [i];
  }
  for (let j = 0; j <= n; j++) {
    dp[0][j] = j;
  }

  for (let i = 1; i <= m; i++) {
    for (let j = 1; j <= n; j++) {
      const cost = a[i - 1] === b[j - 1] ? 0 : 1;
      dp[i][j] = Math.min(
        dp[i - 1][j] + 1,
        dp[i][j - 1] + 1,
        dp[i - 1][j - 1] + cost
      );
    }
  }

  return dp[m][n];
}

export function levenshteinSimilarity(a: string, b: string): number {
  if (a.length === 0 && b.length === 0) return 1;
  const maxLen = Math.max(a.length, b.length);
  if (maxLen === 0) return 1;
  // 使用滑动窗口来匹配
  if (b.length > a.length) [a, b] = [b, a]; // 确保 b 更短

  let bestScore = 0;
  for (let i = 0; i <= a.length - b.length; i++) {
    const window = a.substring(i, i + b.length);
    const dist = levenshteinDistance(b, window);
    const score = 1 - dist / Math.max(b.length, 1);
    if (score > bestScore) bestScore = score;
  }

  return bestScore;
}

export function findAnnotationPosition(
  annotation: Annotation,
  docContent: string
): AnchorMatchResult {
  const lines = docContent.split('\n');

  // 1. 精确匹配
  const exact = tryExactMatch(annotation, lines);
  if (exact) return exact;

  // 2. 模糊匹配
  const fuzzy = tryFuzzyMatch(annotation, lines);
  if (fuzzy && fuzzy.score > 0.7) return fuzzy;

  // 3. 段落级匹配
  const para = tryParagraphMatch(annotation, lines);
  if (para) return para;

  // 4. 丢失
  return {
    status: AnchorStatus.LOST,
    startLine: 0,
    startCh: 0,
    endLine: 0,
    endCh: 0,
    score: 0,
  };
}

function tryExactMatch(
  annotation: Annotation,
  lines: string[]
): AnchorMatchResult | null {
  const target = annotation.targetText;
  if (!target) return null;

  for (let i = 0; i < lines.length; i++) {
    const idx = lines[i].indexOf(target);
    if (idx !== -1) {
      // 验证上下文
      const beforeCtx = lines.slice(Math.max(0, i - 2), i).join('\n');
      const afterCtx = lines.slice(i + 1, Math.min(lines.length, i + 3)).join('\n');

      const matchesBefore =
        !annotation.contextBefore ||
        beforeCtx.endsWith(annotation.contextBefore.slice(-50));
      const matchesAfter =
        !annotation.contextAfter ||
        afterCtx.startsWith(annotation.contextAfter.slice(0, 50));

      if (matchesBefore && matchesAfter) {
        return {
          status: AnchorStatus.EXACT,
          startLine: i,
          startCh: idx,
          endLine: i,
          endCh: idx + target.length,
          score: 1.0,
        };
      }
    }
  }
  return null;
}

function tryFuzzyMatch(
  annotation: Annotation,
  lines: string[]
): AnchorMatchResult | null {
  const target = annotation.targetText;
  if (!target || target.length < 3) return null;

  let best: AnchorMatchResult | null = null;
  let bestScore = 0;

  for (let i = 0; i < lines.length; i++) {
    const line = lines[i];
    const maxJ = Math.max(0, line.length - Math.floor(target.length * 0.5));

    for (let j = 0; j <= maxJ; j++) {
      const end = Math.min(j + Math.ceil(target.length * 1.5), line.length);
      const candidate = line.substring(j, end);
      if (candidate.length < 3) continue;

      const score = levenshteinSimilarity(target, candidate);
      if (score > bestScore && score > 0.7) {
        bestScore = score;
        best = {
          status: AnchorStatus.FUZZY,
          startLine: i,
          startCh: j,
          endLine: i,
          endCh: j + candidate.length,
          score,
        };
      }
    }
  }

  return best;
}

function tryParagraphMatch(
  annotation: Annotation,
  lines: string[]
): AnchorMatchResult | null {
  const target = annotation.targetText;
  if (!target || lines.length === 0) return null;

  // 找包含 target 中第一个有意义词的段落
  const firstWord = target.split(/\s+/).find((w) => w.length > 2);
  if (!firstWord) return null;

  for (let i = 0; i < lines.length; i++) {
    if (lines[i].includes(firstWord)) {
      const paraStart = Math.max(0, i - 1);
      const paraEnd = Math.min(lines.length - 1, i + 1);
      return {
        status: AnchorStatus.PARAGRAPH,
        startLine: paraStart,
        startCh: 0,
        endLine: paraEnd,
        endCh: lines[paraEnd].length,
        score: 0.4,
      };
    }
  }

  return null;
}
```

- [ ] **Step4: 跑测试**

```bash
npx vitest run __tests__/anchor-engine.test.ts
```
预期：PASS

- [ ] **Step5: 提交**

```bash
git add -A && git commit -m "feat: 锚定引擎（内容指纹 + Levenshtein 匹配降级）"
```

---

### 任务 4：AnnotationManager

**文件：** 创建: `src/annotation-manager.ts`  
测试: `__tests__/annotation-manager.test.ts`

**定位锚点：** 新文件，无需锚点

- [ ] **Step1: 创建 src/annotation-manager.ts**

```typescript
import { Annotation, AnnotationType, AnchorStatus, generateId, serializeAnnotation } from './data-models';
import { StorageService } from './storage';
import { computeFingerprint, findAnnotationPosition } from './anchor-engine';

type EventCallback = (...args: any[]) => void;

export class AnnotationManager {
  private annotations: Map<string, Annotation> = new Map();
  private currentFilePath: string = '';
  private eventListeners: Map<string, Set<EventCallback>> = new Map();

  constructor(private storage: StorageService) {}

  on(event: string, callback: EventCallback): void {
    if (!this.eventListeners.has(event)) {
      this.eventListeners.set(event, new Set());
    }
    this.eventListeners.get(event)!.add(callback);
  }

  off(event: string, callback: EventCallback): void {
    this.eventListeners.get(event)?.delete(callback);
  }

  private emit(event: string, ...args: any[]): void {
    this.eventListeners.get(event)?.forEach((cb) => cb(...args));
  }

  async openFile(filePath: string): Promise<void> {
    this.currentFilePath = filePath;
    const loaded = await this.storage.getAllAnnotations(filePath);
    this.annotations.clear();
    for (const anno of loaded) {
      this.annotations.set(anno.id, anno);
    }
    this.emit('file:opened', { path: filePath, count: loaded.length });
  }

  async reanchorAll(docContent: string): Promise<void> {
    for (const [id, anno] of this.annotations) {
      const result = findAnnotationPosition(anno, docContent);
      anno.anchorStatus = result.status;
      anno.startLine = result.startLine;
      anno.startCh = result.startCh;
      anno.endLine = result.endLine;
      anno.endCh = result.endCh;
    }
    this.emit('annotations:reanchored', Array.from(this.annotations.values()));
  }

  createAnnotation(
    type: AnnotationType,
    targetText: string,
    contextBefore: string,
    contextAfter: string,
    options?: { color?: string; commentText?: string }
  ): Annotation | null {
    if (!targetText) return null;

    const fingerprint = computeFingerprint(targetText, contextBefore, contextAfter);
    const annotation: Annotation = {
      id: generateId(),
      type,
      targetText,
      contextBefore,
      contextAfter,
      fingerprint,
      color: options?.color,
      commentText: options?.commentText,
      anchorStatus: AnchorStatus.LOST,
      createdAt: Date.now(),
      updatedAt: Date.now(),
    };

    this.annotations.set(annotation.id, annotation);
    this.emit('annotation:created', annotation);
    return annotation;
  }

  deleteAnnotation(id: string): boolean {
    const deleted = this.annotations.delete(id);
    if (deleted) {
      this.emit('annotation:deleted', { id });
    }
    return deleted;
  }

  updateAnnotation(id: string, updates: Partial<Annotation>): Annotation | null {
    const anno = this.annotations.get(id);
    if (!anno) return null;

    Object.assign(anno, updates, { updatedAt: Date.now() });
    this.emit('annotation:updated', anno);
    return anno;
  }

  getAnnotation(id: string): Annotation | undefined {
    return this.annotations.get(id);
  }

  getAllAnnotations(): Annotation[] {
    return Array.from(this.annotations.values());
  }

  getAnnotationsByFile(filePath: string): Annotation[] {
    return Array.from(this.annotations.values());
  }

  getFilteredAnnotations(
    type?: AnnotationType,
    anchorStatus?: AnchorStatus
  ): Annotation[] {
    let result = Array.from(this.annotations.values());
    if (type) result = result.filter((a) => a.type === type);
    if (anchorStatus) result = result.filter((a) => a.anchorStatus === anchorStatus);
    return result;
  }

  async persist(): Promise<void> {
    if (!this.currentFilePath) return;
    const annotations = Array.from(this.annotations.values());
    const serialized = annotations.map(serializeAnnotation);
    await this.storage.saveAnnotations(this.currentFilePath, annotations);
  }

  getCurrentFilePath(): string {
    return this.currentFilePath;
  }

  clear(): void {
    this.annotations.clear();
    this.currentFilePath = '';
  }
}
```

- [ ] **Step2: 编译验证**

```bash
npx tsc --noEmit --skipLibCheck
```
预期：无编译错误

- [ ] **Step3: 提交**

```bash
git add -A && git commit -m "feat: 批注管理器（CRUD + 事件总线）"
```

---

### 任务 5：编辑模式（FloatingToolbar + CM6 装饰器）

**文件：** 创建: `src/floating-toolbar.ts`, `src/cm6-decorations.ts`

**定位锚点：** 新文件，无需锚点

- [ ] **Step1: 创建 src/floating-toolbar.ts**

```typescript
import { App, Editor, MarkdownView } from 'obsidian';
import { AnnotationType } from './data-models';
import { AnnotationManager } from './annotation-manager';

export class FloatingToolbar {
  private toolbarEl: HTMLElement | null = null;

  constructor(
    private app: App,
    private annotationManager: AnnotationManager
  ) {}

  showOnSelection(editor: Editor): void {
    // 清除已有
    this.hide();

    const selection = editor.getSelection();
    if (!selection || selection.trim().length === 0) return;

    const cursorFrom = editor.getCursor('from');
    const cursorTo = editor.getCursor('to');
    const doc = editor.getDoc();

    // 获取上下文
    const line = editor.getLine(cursorFrom.line);
    const contextBefore = line.substring(0, cursorFrom.ch);
    const contextAfter = line.substring(cursorTo.ch);

    // 获取编辑器视图位置
    const editorEl = this.app.workspace.getActiveViewOfType(MarkdownView)?.editor;
    if (!editorEl) return;

    // 构建浮动工具栏
    this.toolbarEl = document.createElement('div');
    this.toolbarEl.className = 'md-annot-toolbar';

    // 使用坐标定位（基于 cursor 位置）
    const coords = (editor as any).coordsAtPos(
      (editor as any).posToOffset(cursorFrom),
      false
    );
    if (coords) {
      const rect = (editorEl as any).getScrollInfo();
      const offset = (editorEl as any).getScrollInfo();
      this.toolbarEl.style.top = `${coords.top - offset.top - 40}px`;
      this.toolbarEl.style.left = `${coords.left}px`;
    }
    this.toolbarEl.style.position = 'fixed';
    this.toolbarEl.style.zIndex = '1000';

    this.addButton('🖍', 'highlight', '高亮', () => {
      this.annotationManager.createAnnotation(
        AnnotationType.HIGHLIGHT,
        selection,
        contextBefore,
        contextAfter
      );
      this.hide();
    });

    this.addButton('〰️', 'underline', '划线', () => {
      this.annotationManager.createAnnotation(
        AnnotationType.UNDERLINE,
        selection,
        contextBefore,
        contextAfter
      );
      this.hide();
    });

    this.addButton('💬', 'comment', '批注', () => {
      const comment = prompt('输入批注内容：') || '';
      this.annotationManager.createAnnotation(
        AnnotationType.COMMENT,
        selection,
        contextBefore,
        contextAfter,
        { commentText: comment }
      );
      this.hide();
    });

    document.body.appendChild(this.toolbarEl);
  }

  private addButton(
    icon: string,
    type: string,
    label: string,
    onClick: () => void
  ): void {
    const btn = document.createElement('button');
    btn.className = `md-annot-toolbar-btn md-annot-toolbar-${type}`;
    btn.innerHTML = icon;
    btn.title = label;
    btn.addEventListener('click', onClick);
    this.toolbarEl!.appendChild(btn);
  }

  hide(): void {
    this.toolbarEl?.remove();
    this.toolbarEl = null;
  }
}
```

- [ ] **Step2: 创建 src/cm6-decorations.ts**

```typescript
import { EditorSelection, StateField, StateEffect } from '@codemirror/state';
import {
  Decoration,
  DecorationSet,
  EditorView,
  ViewPlugin,
  ViewUpdate,
} from '@codemirror/view';
import { Annotation, AnchorStatus, AnnotationType } from './data-models';

// Effect: 更新装饰集
export const setAnnotationDecorations = StateEffect.define<DecorationSet>();

// 高亮装饰
function highlightDecoration(color: string = '#90EE90') {
  return Decoration.mark({
    class: 'md-annot-highlight',
    attributes: { style: `background-color: ${color}` },
  });
}

// 波浪线装饰
function underlineDecoration() {
  return Decoration.mark({
    class: 'md-annot-underline',
    attributes: { style: 'text-decoration: wavy underline #4169E1;' },
  });
}

// 批注高亮装饰
function commentDecoration(color: string = '#FFFFAA') {
  return Decoration.mark({
    class: 'md-annot-comment',
    attributes: { style: `background-color: ${color}` },
  });
}

// 批注圆点（行号侧标记）
function commentGutterMarker() {
  return Decoration.line({ class: 'md-annot-comment-line' });
}

export function buildDecorations(
  annotations: Annotation[],
  settings: { highlightColor?: string; commentHighlightColor?: string } = {}
): DecorationSet {
  const decos: Decoration[] = [];

  for (const anno of annotations) {
    if (anno.anchorStatus === AnchorStatus.LOST) continue;
    if (anno.startLine === undefined || anno.startCh === undefined) continue;

    const fromLine = anno.startLine;
    const toLine = anno.endLine ?? anno.startLine;
    const fromCh = anno.startCh;
    const toCh = anno.endCh ?? fromCh + anno.targetText.length;

    // 对于段落级别，只标记行
    if (anno.anchorStatus === AnchorStatus.PARAGRAPH) {
      for (let line = fromLine; line <= toLine; line++) {
        decos.push(
          Decoration.line({
            class: 'md-annot-paragraph',
            attributes: { style: 'outline: 2px dashed orange;' },
          }).range(EditorSelection.cursor(line, 0).from)
        );
      }
      continue;
    }

    const docLength = 0; // 占位，实际由 view 计算
    switch (anno.type) {
      case AnnotationType.HIGHLIGHT:
        decos.push(
          highlightDecoration(settings.highlightColor).range(
            EditorSelection.cursor(fromLine, fromCh).from,
            EditorSelection.cursor(toLine, toCh).from
          )
        );
        break;
      case AnnotationType.UNDERLINE:
        decos.push(
          underlineDecoration().range(
            EditorSelection.cursor(fromLine, fromCh).from,
            EditorSelection.cursor(toLine, toCh).from
          )
        );
        break;
      case AnnotationType.COMMENT:
        decos.push(
          commentDecoration(settings.commentHighlightColor).range(
            EditorSelection.cursor(fromLine, fromCh).from,
            EditorSelection.cursor(toLine, toCh).from
          )
        );
        // 行标记
        decos.push(
          commentGutterMarker().range(
            EditorSelection.cursor(fromLine, 0).from
          )
        );
        break;
    }
  }

  return Decoration.set(decos, true);
}

// ViewPlugin: 监听文档变化并更新装饰
export const annotationDecorationsPlugin = ViewPlugin.fromClass(
  class {
    decorations: DecorationSet;

    constructor(view: EditorView) {
      this.decorations = Decoration.none;
    }

    update(update: ViewUpdate) {
      for (const tr of update.transactions) {
        for (const effect of tr.effects) {
          if (effect.is(setAnnotationDecorations)) {
            this.decorations = effect.value;
          }
        }
      }
    }
  },
  { decorations: (v) => v.decorations }
);
```

- [ ] **Step3: 编译验证**

```bash
npx tsc --noEmit --skipLibCheck
```
预期：无编译错误

- [ ] **Step4: 提交**

```bash
git add -A && git commit -m "feat: 浮动工具栏 + CM6 装饰器"
```

---

### 任务 6：侧面板（SidePanel）

**文件：** 创建: `src/side-panel.ts`

**定位锚点：** 新文件，无需锚点

- [ ] **Step1: 创建 src/side-panel.ts**

```typescript
import { App, ItemView, WorkspaceLeaf } from 'obsidian';
import { Annotation, AnnotationType, AnchorStatus } from './data-models';
import { AnnotationManager } from './annotation-manager';

export const VIEW_TYPE_SIDE_PANEL = 'md-annot-side-panel';

export class SidePanelView extends ItemView {
  private annotationManager: AnnotationManager;
  private filterType: AnnotationType | 'all' = 'all';
  private containerEl: HTMLElement;

  constructor(leaf: WorkspaceLeaf, annotationManager: AnnotationManager) {
    super(leaf);
    this.annotationManager = annotationManager;
  }

  getViewType(): string {
    return VIEW_TYPE_SIDE_PANEL;
  }

  getDisplayText(): string {
    return 'MDAnnot 批注面板';
  }

  getIcon(): string {
    return 'highlighter';
  }

  async onOpen(): Promise<void> {
    this.containerEl = this.contentEl;
    this.containerEl.empty();
    this.containerEl.classList.add('md-annot-side-panel');

    // 监听批注事件
    this.annotationManager.on('annotation:created', () => this.refresh());
    this.annotationManager.on('annotation:deleted', () => this.refresh());
    this.annotationManager.on('annotation:updated', () => this.refresh());
    this.annotationManager.on('file:opened', () => this.refresh());

    this.render();
  }

  private render(): void {
    this.containerEl.empty();

    // 标题
    const header = this.containerEl.createDiv({ cls: 'md-annot-panel-header' });
    header.createEl('h3', { text: '批注列表' });

    // 筛选按钮
    const filterBar = this.containerEl.createDiv({ cls: 'md-annot-panel-filter' });
    const filters = ['all', 'highlight', 'underline', 'comment'] as const;
    for (const f of filters) {
      const btn = filterBar.createEl('button', {
        text: f === 'all' ? '全部' : (f === 'highlight' ? '高亮' : f === 'underline' ? '划线' : '批注'),
        cls: `md-annot-filter-btn ${this.filterType === f ? 'is-active' : ''}`,
      });
      btn.addEventListener('click', () => {
        this.filterType = f;
        this.render();
      });
    }

    // 导出按钮
    const exportBtn = this.containerEl.createEl('button', {
      text: '导出 MD',
      cls: 'md-annot-export-btn',
    });
    exportBtn.addEventListener('click', () => this.exportMarkdown());

    // 批注列表
    const listEl = this.containerEl.createDiv({ cls: 'md-annot-panel-list' });
    const annotations = this.getFilteredAnnotations();

    if (annotations.length === 0) {
      listEl.createEl('p', {
        text: '暂无批注',
        cls: 'md-annot-panel-empty',
      });
      return;
    }

    for (const anno of annotations) {
      const item = listEl.createDiv({ cls: 'md-annot-panel-item' });

      // 类型图标
      item.createSpan({
        cls: `md-annot-item-type md-annot-type-${anno.type}`,
        text: this.getTypeIcon(anno.type),
      });

      // 文字摘要
      const textEl = item.createDiv({ cls: 'md-annot-item-text' });
      textEl.setText(anno.targetText.substring(0, 50) + (anno.targetText.length > 50 ? '...' : ''));

      // 锚定状态
      const statusEl = item.createSpan({
        cls: `md-annot-item-status md-annot-status-${anno.anchorStatus}`,
        text: this.getStatusText(anno.anchorStatus),
      });

      // 操作按钮
      const actionsEl = item.createDiv({ cls: 'md-annot-item-actions' });

      const jumpBtn = actionsEl.createEl('button', { text: '跳转', cls: 'md-annot-jump-btn' });
      jumpBtn.addEventListener('click', () => this.jumpToAnnotation(anno));

      const deleteBtn = actionsEl.createEl('button', { text: '删除', cls: 'md-annot-delete-btn' });
      deleteBtn.addEventListener('click', () => {
        this.annotationManager.deleteAnnotation(anno.id);
      });
    }
  }

  private getTypeIcon(type: AnnotationType): string {
    switch (type) {
      case AnnotationType.HIGHLIGHT: return '🖍';
      case AnnotationType.UNDERLINE: return '〰️';
      case AnnotationType.COMMENT: return '💬';
      case AnnotationType.HANDWRITING: return '✏️';
    }
  }

  private getStatusText(status: AnchorStatus): string {
    switch (status) {
      case AnchorStatus.EXACT: return '✅';
      case AnchorStatus.FUZZY: return '⚠️';
      case AnchorStatus.PARAGRAPH: return '📄';
      case AnchorStatus.LOST: return '❌';
    }
  }

  private getFilteredAnnotations(): Annotation[] {
    const all = this.annotationManager.getAllAnnotations();
    if (this.filterType === 'all') return all;
    return all.filter((a) => a.type === this.filterType);
  }

  private jumpToAnnotation(anno: Annotation): void {
    if (anno.startLine !== undefined && this.app.workspace.activeEditor) {
      const editor = (this.app.workspace.activeEditor as any).editor;
      if (editor) {
        editor.setCursor(anno.startLine, anno.startCh || 0);
        editor.focus();
      }
    }
  }

  private exportMarkdown(): void {
    const annotations = this.annotationManager.getAllAnnotations();
    if (annotations.length === 0) return;

    let md = `# 批注导出 - ${this.annotationManager.getCurrentFilePath()}\n\n`;
    md += `导出时间: ${new Date().toLocaleString()}\n\n`;
    md += `| 类型 | 文字 | 状态 | 内容 |\n`;
    md += `|------|------|------|------|\n`;

    for (const anno of annotations) {
      const typeStr = anno.type === 'highlight' ? '高亮' : anno.type === 'underline' ? '划线' : '批注';
      const statusStr = this.getStatusText(anno.anchorStatus);
      const commentStr = anno.commentText || '';
      md += `| ${typeStr} | ${anno.targetText} | ${statusStr} | ${commentStr} |\n`;
    }

    // 创建并下载文件
    const blob = new Blob([md], { type: 'text/markdown' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `annotations-${Date.now()}.md`;
    a.click();
    URL.revokeObjectURL(url);
  }

  refresh(): void {
    this.render();
  }
}
```

- [ ] **Step2: 编译验证**

```bash
npx tsc --noEmit --skipLibCheck
```
预期：无编译错误

- [ ] **Step3: 提交**

```bash
git add -A && git commit -m "feat: 批注侧面板（列表/筛选/跳转/导出）"
```

---

### 任务 7：阅读模式（CanvasOverlay + HandwritingEngine）

**文件：** 创建: `src/handwriting-engine.ts`, `src/canvas-overlay.ts`

**定位锚点：** 新文件，无需锚点

- [ ] **Step1: 创建 src/handwriting-engine.ts**

```typescript
import { Point, Stroke, StrokeTool } from './data-models';

export function catmullRomSpline(
  points: Point[],
  segments: number = 8
): Point[] {
  if (points.length < 2) return points;

  const result: Point[] = [points[0]];

  for (let i = 0; i < points.length - 1; i++) {
    const p0 = points[Math.max(0, i - 1)];
    const p1 = points[i];
    const p2 = points[i + 1];
    const p3 = points[Math.min(points.length - 1, i + 2)];

    for (let t = 1; t <= segments; t++) {
      const tt = t / segments;
      const tt2 = tt * tt;
      const tt3 = tt2 * tt;

      const x =
        0.5 *
        (2 * p1.x +
          (-p0.x + p2.x) * tt +
          (2 * p0.x - 5 * p1.x + 4 * p2.x - p3.x) * tt2 +
          (-p0.x + 3 * p1.x - 3 * p2.x + p3.x) * tt3);

      const y =
        0.5 *
        (2 * p1.y +
          (-p0.y + p2.y) * tt +
          (2 * p0.y - 5 * p1.y + 4 * p2.y - p3.y) * tt2 +
          (-p0.y + 3 * p1.y - 3 * p2.y + p3.y) * tt3);

      const pressure = p1.pressure + (p2.pressure - p1.pressure) * tt;

      result.push({ x, y, pressure, timestamp: p1.timestamp });
    }
  }

  result.push(points[points.length - 1]);
  return result;
}

export function renderStroke(
  ctx: CanvasRenderingContext2D,
  stroke: Stroke,
  smoothness: number = 0.5
): void {
  const { points, color, width, tool } = stroke;
  if (points.length < 2) return;

  // 高亮模式使用更粗的透明度笔触
  if (tool === StrokeTool.HIGHLIGHTER) {
    ctx.strokeStyle = color;
    ctx.lineWidth = width;
    ctx.globalAlpha = 0.3;
    ctx.lineCap = 'butt';
    ctx.lineJoin = 'round';
  } else {
    ctx.strokeStyle = color;
    ctx.lineWidth = width;
    ctx.globalAlpha = 1.0;
    ctx.lineCap = 'round';
    ctx.lineJoin = 'round';
  }

  const smoothed = catmullRomSpline(points, Math.round(8 * smoothness));

  ctx.beginPath();
  ctx.moveTo(smoothed[0].x, smoothed[0].y);

  // 如果支持压感，按压力调整宽度
  if (tool === StrokeTool.PEN) {
    for (let i = 1; i < smoothed.length; i++) {
      const p = smoothed[i];
      const adaptiveWidth = width * (0.5 + p.pressure * 0.5);
      ctx.lineWidth = adaptiveWidth;
      ctx.lineTo(p.x, p.y);
    }
  } else {
    for (let i = 1; i < smoothed.length; i++) {
      ctx.lineTo(smoothed[i].x, smoothed[i].y);
    }
  }

  ctx.stroke();
  ctx.globalAlpha = 1.0;
}
```

- [ ] **Step2: 创建 src/canvas-overlay.ts**

```typescript
import { Stroke, StrokeTool, generateId } from './data-models';
import { renderStroke } from './handwriting-engine';

export interface CanvasOverlayConfig {
  penColor: string;
  penWidth: number;
  highlighterWidth: number;
  smoothness: number;
  activeTool: StrokeTool;
}

export class CanvasOverlay {
  private canvas: HTMLCanvasElement;
  private ctx: CanvasRenderingContext2D;
  private strokes: Stroke[] = [];
  private currentStroke: Stroke | null = null;
  private isDrawing: boolean = false;
  private visible: boolean = true;
  private config: CanvasOverlayConfig;

  private onStrokesChange: ((strokes: Stroke[]) => void) | null = null;

  constructor(
    container: HTMLElement,
    config: CanvasOverlayConfig,
    onStrokesChange?: (strokes: Stroke[]) => void
  ) {
    this.config = config;
    this.onStrokesChange = onStrokesChange || null;

    this.canvas = document.createElement('canvas');
    this.canvas.className = 'md-annot-canvas-overlay';

    // 覆盖在阅读视图上方
    this.canvas.style.cssText = `
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: auto;
      z-index: 100;
    `;

    this.ctx = this.canvas.getContext('2d')!;
    container.appendChild(this.canvas);
    this.initEvents();
  }

  private initEvents(): void {
    this.canvas.addEventListener('pointerdown', this.onPointerDown.bind(this));
    this.canvas.addEventListener('pointermove', this.onPointerMove.bind(this));
    this.canvas.addEventListener('pointerup', this.onPointerUp.bind(this));
    this.canvas.addEventListener('pointerleave', this.onPointerUp.bind(this));

    // 窗口大小变化时调整 canvas
    const resizeObserver = new ResizeObserver(() => this.handleResize());
    resizeObserver.observe(this.canvas.parentElement!);

    this.handleResize();
  }

  private handleResize(): void {
    const parent = this.canvas.parentElement;
    if (!parent) return;

    const rect = parent.getBoundingClientRect();
    this.canvas.width = rect.width * devicePixelRatio;
    this.canvas.height = rect.height * devicePixelRatio;
    this.canvas.style.width = `${rect.width}px`;
    this.canvas.style.height = `${rect.height}px`;
    this.ctx.scale(devicePixelRatio, devicePixelRatio);

    this.render();
  }

  private onPointerDown(e: PointerEvent): void {
    if (!this.visible) return;
    this.isDrawing = true;
    this.canvas.setPointerCapture(e.pointerId);

    this.currentStroke = {
      id: generateId(),
      points: [],
      color: this.config.penColor,
      width:
        this.config.activeTool === StrokeTool.HIGHLIGHTER
          ? this.config.highlighterWidth
          : this.config.penWidth,
      tool: this.config.activeTool,
    };

    this.addPoint(e);
  }

  private onPointerMove(e: PointerEvent): void {
    if (!this.isDrawing || !this.currentStroke) return;
    this.addPoint(e);

    // 实时渲染
    this.render();
  }

  private onPointerUp(e: PointerEvent): void {
    if (!this.isDrawing || !this.currentStroke) return;
    this.isDrawing = false;

    if (this.currentStroke.points.length > 0) {
      this.strokes.push(this.currentStroke);
      this.currentStroke = null;
      this.onStrokesChange?.(this.strokes);
    }
  }

  private addPoint(e: PointerEvent): void {
    const rect = this.canvas.getBoundingClientRect();
    this.currentStroke!.points.push({
      x: e.clientX - rect.left,
      y: e.clientY - rect.top,
      pressure: e.pressure || 0.5,
      timestamp: Date.now(),
    });
  }

  render(): void {
    const width = this.canvas.width / devicePixelRatio;
    const height = this.canvas.height / devicePixelRatio;

    this.ctx.clearRect(0, 0, width, height);

    // 渲染已完成笔触
    for (const stroke of this.strokes) {
      renderStroke(this.ctx, stroke, this.config.smoothness);
    }

    // 渲染当前笔触
    if (this.currentStroke) {
      renderStroke(this.ctx, this.currentStroke, this.config.smoothness);
    }
  }

  setStrokes(strokes: Stroke[]): void {
    this.strokes = strokes;
    this.render();
  }

  getStrokes(): Stroke[] {
    return [...this.strokes];
  }

  setVisible(visible: boolean): void {
    this.visible = visible;
    this.canvas.style.display = visible ? 'block' : 'none';
  }

  setConfig(config: Partial<CanvasOverlayConfig>): void {
    Object.assign(this.config, config);
  }

  destroy(): void {
    this.canvas.remove();
  }
}
```

- [ ] **Step3: 编译验证**

```bash
npx tsc --noEmit --skipLibCheck
```
预期：无编译错误

- [ ] **Step4: 提交**

```bash
git add -A && git commit -m "feat: Canvas覆盖层 + 手写引擎（Catmull-Rom + 压感）"
```

---

### 任务 8：SettingsTab + main.ts 集成

**文件：** 创建: `src/settings-tab.ts`, 修改 `src/main.ts`

**定位锚点：** 新文件无需锚点。`main.ts` 在最后一行 `}` 之前追加集成代码。

- [ ] **Step1: 创建 src/settings-tab.ts**

```typescript
import { App, PluginSettingTab, Setting } from 'obsidian';
import MDAnnotPlugin from './main';
import { MDAnnotSettings, DEFAULT_SETTINGS } from './data-models';

export class MDAnnotSettingTab extends PluginSettingTab {
  constructor(app: App, private plugin: MDAnnotPlugin) {
    super(app, plugin);
  }

  display(): void {
    const { containerEl } = this;
    containerEl.empty();

    containerEl.createEl('h2', { text: 'MDAnnot 设置' });

    // 显示控制
    containerEl.createEl('h3', { text: '显示控制' });

    new Setting(containerEl)
      .setName('编辑模式显示批注')
      .setDesc('在编辑模式下显示文本批注标记')
      .addToggle((toggle) =>
        toggle
          .setValue(this.plugin.settings.showInEditor)
          .onChange(async (val) => {
            this.plugin.settings.showInEditor = val;
            await this.plugin.saveSettings();
          })
      );

    new Setting(containerEl)
      .setName('阅读模式显示批注')
      .setDesc('在阅读模式下显示手写批注')
      .addToggle((toggle) =>
        toggle
          .setValue(this.plugin.settings.showInReading)
          .onChange(async (val) => {
            this.plugin.settings.showInReading = val;
            await this.plugin.saveSettings();
          })
      );

    new Setting(containerEl)
      .setName('自动显示批注面板')
      .setDesc('文档存在批注时自动显示侧面板')
      .addToggle((toggle) =>
        toggle
          .setValue(this.plugin.settings.autoShowPanel)
          .onChange(async (val) => {
            this.plugin.settings.autoShowPanel = val;
            await this.plugin.saveSettings();
          })
      );

    // 批注样式
    containerEl.createEl('h3', { text: '批注样式' });

    new Setting(containerEl)
      .setName('高亮颜色')
      .addText((text) =>
        text
          .setValue(this.plugin.settings.highlightColor)
          .onChange(async (val) => {
            this.plugin.settings.highlightColor = val;
            await this.plugin.saveSettings();
          })
      );

    new Setting(containerEl)
      .setName('划线颜色')
      .addText((text) =>
        text
          .setValue(this.plugin.settings.underlineColor)
          .onChange(async (val) => {
            this.plugin.settings.underlineColor = val;
            await this.plugin.saveSettings();
          })
      );

    new Setting(containerEl)
      .setName('批注高亮颜色')
      .addText((text) =>
        text
          .setValue(this.plugin.settings.commentHighlightColor)
          .onChange(async (val) => {
            this.plugin.settings.commentHighlightColor = val;
            await this.plugin.saveSettings();
          })
      );

    // 手写输入
    containerEl.createEl('h3', { text: '手写输入' });

    new Setting(containerEl)
      .setName('默认笔触颜色')
      .addText((text) =>
        text
          .setValue(this.plugin.settings.defaultPenColor)
          .onChange(async (val) => {
            this.plugin.settings.defaultPenColor = val;
            await this.plugin.saveSettings();
          })
      );

    new Setting(containerEl)
      .setName('默认笔触粗细')
      .addSlider((slider) =>
        slider
          .setLimits(1, 10, 1)
          .setValue(this.plugin.settings.defaultPenWidth)
          .onChange(async (val) => {
            this.plugin.settings.defaultPenWidth = val;
            await this.plugin.saveSettings();
          })
      );

    new Setting(containerEl)
      .setName('笔触平滑度')
      .addSlider((slider) =>
        slider
          .setLimits(0.1, 1, 0.1)
          .setValue(this.plugin.settings.smoothness)
          .onChange(async (val) => {
            this.plugin.settings.smoothness = val;
            await this.plugin.saveSettings();
          })
      );

    // 数据存储
    containerEl.createEl('h3', { text: '数据存储' });

    new Setting(containerEl)
      .setName('保留历史版本')
      .addToggle((toggle) =>
        toggle
          .setValue(this.plugin.settings.keepHistory)
          .onChange(async (val) => {
            this.plugin.settings.keepHistory = val;
            await this.plugin.saveSettings();
          })
      );

    new Setting(containerEl)
      .setName('最大历史版本数')
      .addSlider((slider) =>
        slider
          .setLimits(5, 100, 5)
          .setValue(this.plugin.settings.maxHistoryCount)
          .onChange(async (val) => {
            this.plugin.settings.maxHistoryCount = val;
            await this.plugin.saveSettings();
          })
      );

    new Setting(containerEl)
      .setName('自动保存间隔（秒）')
      .addSlider((slider) =>
        slider
          .setLimits(10, 120, 5)
          .setValue(this.plugin.settings.autoSaveInterval)
          .onChange(async (val) => {
            this.plugin.settings.autoSaveInterval = val;
            await this.plugin.saveSettings();
          })
      );

    // Markdown 互操作
    containerEl.createEl('h3', { text: 'Markdown 互操作' });

    new Setting(containerEl)
      .setName('同步高亮到 Markdown')
      .setDesc('将高亮同步写入 `==高亮==` 语法（默认关闭）')
      .addToggle((toggle) =>
        toggle
          .setValue(this.plugin.settings.syncHighlightToMD)
          .onChange(async (val) => {
            this.plugin.settings.syncHighlightToMD = val;
            await this.plugin.saveSettings();
          })
      );

    new Setting(containerEl)
      .setName('同步划线到 Markdown')
      .setDesc('将划线同步写入 `~~删除线~~` 语法（默认关闭）')
      .addToggle((toggle) =>
        toggle
          .setValue(this.plugin.settings.syncUnderlineToMD)
          .onChange(async (val) => {
            this.plugin.settings.syncUnderlineToMD = val;
            await this.plugin.saveSettings();
          })
      );
  }
}
```

- [ ] **Step2: 重写 src/main.ts**

```typescript
import { App, Plugin, PluginSettingTab, Setting, MarkdownView, WorkspaceLeaf, Editor } from 'obsidian';
import { AnnotationManager } from './annotation-manager';
import { StorageService } from './storage';
import { FloatingToolbar } from './floating-toolbar';
import { SidePanelView, VIEW_TYPE_SIDE_PANEL } from './side-panel';
import { CanvasOverlay, CanvasOverlayConfig } from './canvas-overlay';
import { StrokeTool } from './data-models';
import { MDAnnotSettingTab } from './settings-tab';
import { MDAnnotSettings, DEFAULT_SETTINGS } from './data-models';
import { annotationDecorationsPlugin, setAnnotationDecorations, buildDecorations } from './cm6-decorations';
import { EditorView } from '@codemirror/view';

export default class MDAnnotPlugin extends Plugin {
  settings: MDAnnotSettings = DEFAULT_SETTINGS;
  storage: StorageService;
  annotationManager: AnnotationManager;
  floatingToolbar: FloatingToolbar;
  sidePanelView: SidePanelView | null = null;
  canvasOverlay: CanvasOverlay | null = null;

  async onload() {
    await this.loadSettings();

    this.storage = new StorageService(this.app.vault);
    this.annotationManager = new AnnotationManager(this.storage);
    this.floatingToolbar = new FloatingToolbar(this.app, this.annotationManager);

    // 注册 CM6 扩展
    this.registerEditorExtension(annotationDecorationsPlugin);

    // 注册侧面板视图
    this.registerView(VIEW_TYPE_SIDE_PANEL, (leaf: WorkspaceLeaf) => {
      this.sidePanelView = new SidePanelView(leaf, this.annotationManager);
      return this.sidePanelView;
    });

    // Ribbon 图标 - 切换侧面板
    this.addRibbonIcon('highlighter', 'MDAnnot 批注面板', () => {
      this.toggleSidePanel();
    });

    // 命令
    this.addCommand({
      id: 'toggle-annot-panel',
      name: '切换批注面板',
      hotkeys: [{ modifiers: ['Mod', 'Shift'], key: 'A' }],
      callback: () => this.toggleSidePanel(),
    });

    this.addCommand({
      id: 'toggle-handwriting',
      name: '切换手写模式',
      callback: () => this.toggleHandwriting(),
    });

    // 设置面板
    this.addSettingTab(new MDAnnotSettingTab(this.app, this));

    // 监听事件
    this.registerEvent(
      this.app.workspace.on('active-leaf-change', () => this.onActiveLeafChange())
    );

    this.registerEvent(
      this.app.workspace.on('editor-change', () => this.onEditorChange())
    );

    // 编辑器菜单事件 - 选中文字时显示工具栏
    this.registerDomEvent(document, 'selectionchange', () => {
      this.onSelectionChange();
    });

    console.log('MDAnnot: loaded');
  }

  async onunload() {
    // 保存当前批注
    await this.annotationManager.persist();

    // 清理 Canvas
    this.canvasOverlay?.destroy();

    // 卸载侧面板
    this.app.workspace.detachLeavesOfType(VIEW_TYPE_SIDE_PANEL);

    console.log('MDAnnot: unloaded');
  }

  async loadSettings(): Promise<void> {
    this.settings = Object.assign({}, DEFAULT_SETTINGS, await this.loadData());
  }

  async saveSettings(): Promise<void> {
    await this.saveData(this.settings);
  }

  private async toggleSidePanel(): Promise<void> {
    const existing = this.app.workspace.getLeavesOfType(VIEW_TYPE_SIDE_PANEL);
    if (existing.length > 0) {
      this.app.workspace.detachLeavesOfType(VIEW_TYPE_SIDE_PANEL);
    } else {
      const leaf = this.app.workspace.getRightLeaf(false);
      if (leaf) {
        await leaf.setViewState({ type: VIEW_TYPE_SIDE_PANEL, active: true });
      }
    }
  }

  private async onActiveLeafChange(): Promise<void> {
    const view = this.app.workspace.getActiveViewOfType(MarkdownView);
    if (!view) return;

    const filePath = view.file?.path || '';

    // 加载批注
    await this.annotationManager.openFile(filePath);

    // 重新锚定
    const content = view.data;
    await this.annotationManager.reanchorAll(content);

    // 更新 CM6 装饰
    this.updateCM6Decorations(view);

    // 初始化 Canvas（阅读模式）
    this.initCanvasOverlay(view);

    // 自动显示侧面板
    if (this.settings.autoShowPanel && this.annotationManager.getAllAnnotations().length > 0) {
      const existing = this.app.workspace.getLeavesOfType(VIEW_TYPE_SIDE_PANEL);
      if (existing.length === 0) {
        const leaf = this.app.workspace.getRightLeaf(false);
        if (leaf) {
          await leaf.setViewState({ type: VIEW_TYPE_SIDE_PANEL, active: true });
        }
      }
    }
  }

  private onEditorChange(): void {
    const view = this.app.workspace.getActiveViewOfType(MarkdownView);
    if (!view) return;

    const content = view.data;
    this.annotationManager.reanchorAll(content).then(() => {
      this.updateCM6Decorations(view);
    });
  }

  private onSelectionChange(): void {
    if (!this.settings.showInEditor) return;

    const view = this.app.workspace.getActiveViewOfType(MarkdownView);
    if (!view || view.getMode() !== 'source') return;

    const editor = view.editor;
    const selection = editor.getSelection();
    if (selection && selection.trim().length > 0) {
      this.floatingToolbar.showOnSelection(editor);
    }
  }

  private updateCM6Decorations(view: MarkdownView): void {
    const cm = (view as any).editor?.cm as EditorView;
    if (!cm) return;

    const annotations = this.annotationManager.getAllAnnotations();
    const decorations = buildDecorations(annotations, {
      highlightColor: this.settings.highlightColor,
      commentHighlightColor: this.settings.commentHighlightColor,
    });

    cm.dispatch({
      effects: setAnnotationDecorations.of(decorations),
    });
  }

  private initCanvasOverlay(view: MarkdownView): void {
    // 清理旧的 Canvas
    this.canvasOverlay?.destroy();

    // 只在预览模式下显示
    if (view.getMode() !== 'preview') return;

    const previewEl = view.previewMode?.containerEl;
    if (!previewEl) return;

    const config: CanvasOverlayConfig = {
      penColor: this.settings.defaultPenColor,
      penWidth: this.settings.defaultPenWidth,
      highlighterWidth: this.settings.defaultHighlighterWidth,
      smoothness: this.settings.smoothness,
      activeTool: StrokeTool.PEN,
    };

    this.canvasOverlay = new CanvasOverlay(previewEl, config, (strokes) => {
      // 手写完成后的回调
    });

    this.canvasOverlay.setVisible(this.settings.showInReading);
  }

  private toggleHandwriting(): void {
    if (!this.canvasOverlay) return;
    const currentVisible = this.canvasOverlay.getStrokes().length > 0;
    this.canvasOverlay.setVisible(!currentVisible);
  }
}
```

- [ ] **Step3: 编译验证**

```bash
npx tsc --noEmit --skipLibCheck
```
预期：无编译错误

- [ ] **Step4: 提交**

```bash
git add -A && git commit -m "feat: 设置面板 + 插件主集成"
```

---

### 任务 9：CSS 样式

**文件：** 创建: `styles.css`

- [ ] **Step1: 创建 styles.css**

```css
/* MDAnnot - Obsidian Markdown Handwriting Annotation Plugin */

/* 浮动工具栏 */
.md-annot-toolbar {
  position: fixed;
  display: flex;
  gap: 4px;
  padding: 6px 8px;
  background: var(--background-primary);
  border: 1px solid var(--background-modifier-border);
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
  z-index: 1000;
  align-items: center;
}

.md-annot-toolbar-btn {
  width: 32px;
  height: 32px;
  border: none;
  border-radius: 6px;
  background: transparent;
  cursor: pointer;
  font-size: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background 0.15s;
}

.md-annot-toolbar-btn:hover {
  background: var(--background-modifier-hover);
}

/* CM6 装饰 */
.md-annot-highlight {
  background-color: #90EE90;
  border-radius: 2px;
}

.md-annot-underline {
  text-decoration: wavy underline #4169E1;
  text-underline-offset: 2px;
}

.md-annot-comment {
  background-color: #FFFFAA;
  border-radius: 2px;
}

.md-annot-comment-line .cm-gutterElement::after {
  content: '•';
  color: #FFA500;
  font-size: 14px;
  position: absolute;
  right: 4px;
}

.md-annot-paragraph {
  outline: 2px dashed #FFA500;
  outline-offset: -2px;
}

/* 锚定状态 */
.md-annot-status-exact { color: #4CAF50; }
.md-annot-status-fuzzy { color: #FF9800; }
.md-annot-status-paragraph { color: #FF9800; }
.md-annot-status-lost { color: #F44336; }

/* 侧面板 */
.md-annot-side-panel {
  padding: 16px;
  height: 100%;
  overflow-y: auto;
}

.md-annot-panel-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 12px;
}

.md-annot-panel-header h3 {
  margin: 0;
  font-size: 16px;
}

.md-annot-panel-filter {
  display: flex;
  gap: 4px;
  margin-bottom: 12px;
  flex-wrap: wrap;
}

.md-annot-filter-btn {
  padding: 4px 10px;
  border: 1px solid var(--background-modifier-border);
  border-radius: 4px;
  background: transparent;
  cursor: pointer;
  font-size: 12px;
}

.md-annot-filter-btn.is-active {
  background: var(--interactive-accent);
  color: var(--text-on-accent);
}

.md-annot-export-btn {
  width: 100%;
  padding: 6px;
  margin-bottom: 12px;
  border: 1px solid var(--background-modifier-border);
  border-radius: 4px;
  background: transparent;
  cursor: pointer;
  font-size: 12px;
}

.md-annot-export-btn:hover {
  background: var(--background-modifier-hover);
}

.md-annot-panel-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.md-annot-panel-empty {
  color: var(--text-muted);
  text-align: center;
  padding: 20px;
}

.md-annot-panel-item {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 10px;
  border: 1px solid var(--background-modifier-border);
  border-radius: 6px;
  background: var(--background-secondary);
}

.md-annot-item-type {
  font-size: 14px;
}

.md-annot-item-text {
  font-size: 13px;
  word-break: break-all;
  line-height: 1.4;
}

.md-annot-item-status {
  font-size: 12px;
}

.md-annot-item-actions {
  display: flex;
  gap: 6px;
  margin-top: 4px;
}

.md-annot-jump-btn,
.md-annot-delete-btn {
  padding: 2px 8px;
  border: 1px solid var(--background-modifier-border);
  border-radius: 4px;
  background: transparent;
  cursor: pointer;
  font-size: 11px;
}

.md-annot-jump-btn:hover {
  background: var(--interactive-accent);
  color: var(--text-on-accent);
}

.md-annot-delete-btn:hover {
  background: #F44336;
  color: white;
}

/* Canvas 覆盖层 */
.md-annot-canvas-overlay {
  position: absolute;
  top: 0;
  left: 0;
  pointer-events: auto;
  z-index: 100;
}
```

- [ ] **Step2: 编译验证**

```bash
npm run build
```
预期：main.js + styles.css 成功输出无错误

- [ ] **Step3: 提交**

```bash
git add -A && git commit -m "feat: CSS 样式"
```

---

## 执行验证

```
- [ ] 编译：npx tsc --noEmit --skipLibCheck → 无错误
- [ ] 构建：npm run build → main.js + styles.css 成功输出
- [ ] 单元测试：npx vitest run → BUILD SUCCESS
- [ ] 在 Obsidian 中加载插件 → 能识别 md-annot 插件
- [ ] 打开 Markdown 文件 → 选中文字出现浮动工具栏
- [ ] 创建高亮/划线/批注 → 在侧面板可见
- [ ] 修改文本后批注正确降级展示
- [ ] 阅读模式下 Canvas 覆盖层可见，可手写
- [ ] 所有 Plan 任务标记完成
```

## 执行交接

> **内联执行** → 使用 tech_loop_exec_plan 逐任务执行，每任务执行后跑编译验证。
> 完成后更新计划进度，标记已完成步骤。
